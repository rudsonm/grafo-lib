angular.min.js: framework para bind do html <-> javascript

grafo.controller.js: responsável pela interface, chamadas de funções, etc..

grafo.instances.js: instancias de grafos pré prontas.

grafo.utils.js: funções de busca e funções auxiliares para as buscas.

grafo.js: estrutura de grafo.
